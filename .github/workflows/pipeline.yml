name: CI/CD Pipeline

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'temurin'
          

      - name: Install Pre-commit
        run: |
          pip install pre-commit

      - name: Run Pre-commit
        run: |
          pre-commit run --all-files
          
      - name: Install OWASP Dependency-Check
        run: |
          wget https://github.com/jeremylong/DependencyCheck/releases/download/v10.0.4/dependency-check-10.0.4-release.zip
          unzip dependency-check-10.0.4-release.zip -d /opt/
          mv /opt/dependency-check /opt/dependency-check-install

      - name: Run OWASP Dependency-Check
        run: |
          /opt/dependency-check-install/bin/dependency-check.sh --project "simple" --format ALL --failOnCVSS 9
          
      - name: Scan Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Dockerfile
          verbose: true
          
      
      - name: Increase version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "Current version: ${VERSION}"
          PATCH=$(echo $VERSION | cut -d '.' -f 3 | cut -d '-' -f 1)
          echo "Current patch version: ${PATCH}"
          INCREASED_VERSION=1.0.$((PATCH + 1))-SNAPSHOT
          echo "Changing version to: ${INCREASED_VERSION}"
          mvn versions:set -DnewVersion=${INCREASED_VERSION}
          mvn versions:commit 
          cat pom.xml
      
          
      - name: Login to Docker registry
        uses: docker/login-action@v3
        with:
          username: orgitlab
          password: ${{ secrets.DOCKER_PASSWORD }} # pragma: allowlist secret
      
      - name: Push Docker image to registry
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            orgitlab/hello:${{ github.sha }}
            orgitlab/hello:latest
            
      - name: Run Snyk to check Docker image for vulnerabilities
        uses: snyk/actions/docker@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          image: orgitlab/hello:${{ github.sha }}
          args: --severity-threshold=high
     
      - name: Install Cosign
        run: |
          wget https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64 -O /usr/local/bin/cosign
          chmod +x /usr/local/bin/cosign
         

      - name: Create keys for Cosign
      
        run: |
          cosign generate-key-pair
          echo "cosign_key=$(cat cosign.key)" >> $GITHUB_ENV
          echo "cosign_pub_key=$(cat cosign.pub)" >> $GITHUB_ENV
          

      - name: Sign Docker Image
        run: |
          echo "${{ env.cosign_key }}" > temp_cosign.key
          cosign sign --key temp_cosign.key orgitlab/hello:${{ github.sha }}

      - name: Verify Docker Image
        run: |
          echo "${{ env.cosign_pub_key }}" > temp_cosign.pub
          cosign verify --key temp_cosign.pub orgitlab/hello:${{ github.sha }}
            
      - name: Commit updated pom.xml
        run: |
          git config --local user.name "GitHub Actions"
          git config --local user.email "actions@github.com"
          git add pom.xml
          git commit -m "Update pom.xml version"
          git pull origin master --rebase
          git push origin HEAD:master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}      
